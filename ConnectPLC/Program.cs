/*var builder = WebApplication.CreateBuilder(args);

// Add services to the container.

builder.Services.AddControllers();
// Learn more about configuring Swagger/OpenAPI at https://aka.ms/aspnetcore/swashbuckle
builder.Services.AddEndpointsApiExplorer();
builder.Services.AddSwaggerGen();

var app = builder.Build();

// Configure the HTTP request pipeline.
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseHttpsRedirection();

app.UseAuthorization();

app.MapControllers();

app.Run();
*/

using S7.Net;

// 1. ??????????????????? (?????? LOGO! 8.3 ?????? Logo0BA8)
// "192.168.x.x" ??? IP ??? LOGO!, Rack 0, Slot 1
Plc logo = new Plc(CpuType.Logo0BA8, "192.168.1.101", 0, 1);

try
{
    logo.Open();
    if (logo.IsConnected)
    {
        Console.WriteLine("???????????? LOGO! ??????");

        // 2. ?????????? VM Mapping 
        // ?????????? Address ?? LOGO! ???? 2 ??????????? Word:
        // ???????????? "DB1.DBW2" (DBW ??? Data Block Word)
        ushort countValue = (ushort)logo.Read("DB1.DBW2");

        Console.WriteLine($"??????????????????????: {countValue}");

        // 3. (Optional) ????????????????? Server ???? Database ??????
        // SaveToMyDatabase(countValue);
    }
}
catch (Exception ex)
{
    Console.WriteLine($"??????????????: {ex.Message}");
}
finally
{
    logo.Close();
}